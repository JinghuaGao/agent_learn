# 周工作总结（2026-03-19）

## 一、本周工作概览

本周我主要完成了两件事：

1. 学习并搭建了可运行的 Agent 系统（基于 AutoGen）。
2. 启动了 RAG 评估方向学习（Ragas / TruLens）。

本周重点产出是 [replace_rag/agent_replace_rag.py](replace_rag/agent_replace_rag.py) 的实验闭环：
- Agent 可调用工具完成检索与阅读；
- 可基于 PDF 证据给出结论；
- 在目标问题上实现了有效召回。

---

## 二、从增强型 LLM 到 Workflow 再到 Agent

### 1) 增强型 LLM（Augmented LLM）

增强型 LLM 本质是“模型 + 外部能力”：
- 检索（retrieval）
- 工具调用（tools）
- 记忆（memory）

它通常还是单次或少量轮次决策，重点是“能力增强”。

### 2) Workflow（工作流）

Workflow 是把任务流程“提前设计好”：
- 步骤固定（如检索 → 生成 → 校验）
- 路由固定（某类问题走某条链路）
- 可做并行与投票

优点是可控、可复现；缺点是灵活性有限。

### 3) Agent（智能体）

Agent 是“模型在环境反馈中动态决策”：
- 可自己决定下一步调用哪个工具
- 可在多轮中迭代修正
- 可处理步骤数难以预估的开放任务

优点是灵活；代价是成本与不确定性更高，因此需要更强的可观测与护栏。

### 4) 三者区别（简要）

- 增强型 LLM：增强能力，不强调复杂自主流程。
- Workflow：流程预定义，强调稳定与可控。
- Agent：流程动态生成，强调适应复杂任务。

---

## 三、我为什么选择 AutoGen

本项目选择 AutoGen，主要原因有四点：

1. **工具调用链路清晰**：适合快速搭建“计划-执行-观察”闭环。
2. **工程扩展性好**：可从单 Agent 起步，再扩展到多 Agent 协作。
3. **生态兼容性高**：支持 OpenAI-compatible 模型接入，便于切换模型服务。
4. **贴合当前阶段目标**：先跑通最小可用系统，再逐步补可观测、评估与鲁棒性。

本周实现可参考 [replace_rag/agent_replace_rag.py](replace_rag/agent_replace_rag.py)。

---

## 四、replace_rag 实验说明（本周核心产出）

### 1) 实验目标

构建一个“可调用工具的论文问答 Agent”，在不依赖重度预处理流水线的情况下完成检索与回答。

### 2) 当前实现

Agent 接入了最小工具集（目录检查、读取、检索、元数据管理、PDF精读等），包括：
- `fs_pwd`、`fs_list_tree`、`fs_read`
- `metadata_check_freshness`、`metadata_compact_previews`、`metadata_retrieve_top_docs`
- `docling_read_pdf`、`pdf_search`

### 3) 实验结果

- 主流程已跑通；
- 在目标问题上完成有效召回；
- 能给出“结论 + 证据 + 不确定性 + 后续建议”的结构化回答。

### 4) 关键参数与工程经验（本周新增）

#### `max_tool_iterations` 是稳定性关键参数

在 [replace_rag/agent_replace_rag.py](replace_rag/agent_replace_rag.py) 中，我将 `max_tool_iterations` 设为 `5`（见 [replace_rag/agent_replace_rag.py](replace_rag/agent_replace_rag.py#L132)）。

这个参数直接决定单次任务里 Agent 可进行的工具调用轮数上限，影响很大：
- 设得太小：还没完成“粗筛文档 → 页级定位 → 证据整合”就提前停止；
- 设得太大：可能导致无效循环、时延和成本上升。

当前阶段我的经验是：在论文问答场景，`5` 轮是一个可用的折中值，后续应结合任务复杂度做动态调整。

#### 真实踩坑：模型会产生“工具调用幻觉”

本周最典型的问题不是工具不可用，而是模型偶发输出伪工具调用文本（例如伪 `<tool_call>`），看起来像“要调用工具”，但并没有真实执行。

我当前的修正策略：
- 在系统提示里明确约束“只能通过真实工具调用，不要输出伪工具调用文本”；
- 以真实工具事件与返回结果作为判定依据，而不是仅看模型文本；
- 当证据不足时强制输出“证据不足”，避免模型在无真实工具结果时编造答案。

这部分经验说明：Agent 工程里“工具协议稳定性”与“参数控制”同等重要，甚至比单纯优化提示词更关键。

### 5) 失败案例复盘（Before / After）

#### Case A：工具调用轮次不足导致“半途而废”

- **Before（问题表现）**：
	- Agent 在完成文档粗筛后就触发结束，未进入页级证据定位；
	- 输出结论泛化，证据链不完整。
- **定位原因**：
	- 工具调用轮次上限过低，复杂问题在中间阶段被截断。
- **After（改进动作）**：
	- 将 `max_tool_iterations` 调整为 `5`，并在提示词中强调“证据不足不得下结论”。
- **结果变化**：
	- 能完成“粗筛 → 精读/检索 → 回答”的完整闭环；
	- 结构化回答中的证据项明显更完整。

#### Case B：伪工具调用文本导致“看起来在调用、实际未执行”

- **Before（问题表现）**：
	- 模型偶发输出伪 `<tool_call>` 文本；
	- 从表面看像在调用工具，实际没有真实工具执行结果。
- **定位原因**：
	- 模型在 function-calling 协议上发生漂移，文本模仿了工具格式。
- **After（改进动作）**：
	- 系统提示明确禁止伪工具调用；
	- 以真实工具事件与返回值作为唯一执行依据；
	- 无证据时强制输出“证据不足”。
- **结果变化**：
	- 工具执行链路更可判定；
	- 幻觉性“伪执行”显著减少，答案可信度提升。

#### 复盘结论

对 Agent-RAG 来说，稳定性提升并不只靠“更强模型”，而是靠：
- 参数边界（如 `max_tool_iterations`）
- 工具协议约束（禁止伪调用）
- 证据优先的输出规则（无证据不下结论）

---

## 五、与传统 RAG 的对比（当前阶段观察）

### 传统 RAG 常见路径

通常需要较重的数据预处理：
- 文档清洗
- 分块策略调参
- 向量化与索引维护

### replace_rag 的思路

- 降低对“前置重清洗/固定分块”的依赖；
- 让 Agent 在任务中动态选择“先粗筛文档，再精读页证据”；
- 使用 `docling_read_pdf` 辅助模型直接读取 PDF 内容。

### 当前结论

该方案在探索阶段已验证可行，尤其适合快速实验和复杂问题分析；
但在大规模生产场景，仍需继续补齐评估、成本控制与稳定性治理。

---

## 六、主流 Agent 框架对比（阶段性结论）

> 本节目标不是做“绝对排名”，而是给出当前阶段的选型参考。

### 1) AutoGen

- **定位**：多 Agent 协作与工具调用框架，适合从单 Agent 到多 Agent 逐步扩展。
- **优势**：
	- Agent 抽象清晰，便于构建“计划-执行-观察”闭环；
	- 工具调用与会话编排能力较完整；
	- 便于和 OpenAI-compatible 模型接入。
- **注意点**：
	- 需要自己补齐评估、观测、治理策略；
	- 实际稳定性高度依赖工具协议与系统提示设计。

### 2) LangChain / LangGraph

- **定位**：以组件化链路（LangChain）和图编排状态机（LangGraph）见长。
- **优势**：
	- 生态广、集成多、资料丰富；
	- 对“固定流程 + 明确状态迁移”的任务非常友好。
- **注意点**：
	- 在复杂场景下抽象层较多，调试成本可能上升；
	- 需警惕为框架而框架，避免过度工程化。

### 3) AutoGPT（含同类自主代理范式）

- **定位**：强调高自主性、长链路任务自动执行。
- **优势**：
	- 适合开放任务探索与原型验证；
	- 能快速展示“自主执行”效果。
- **注意点**：
	- 成本、时延、错误累积风险较高；
	- 在生产环境常需加强约束与人工审查。

### 4) Anthropic Agent SDK（及其方法论）

- **定位**：强调从简单模式起步，逐步增加复杂度；重视工具接口（ACI）设计。
- **优势**：
	- 方法论清晰（augmented LLM → workflow → agent）；
	- 强调可解释、可观测与防错设计。
- **注意点**：
	- 更偏工程原则与实践范式，落地时仍需结合现有技术栈。

### 5) 异同总结（我当前的理解）

- **共同点**：
	- 都围绕“模型 + 工具 + 反馈循环”；
	- 都需要评估体系和可观测性支撑，才能走向稳定生产。
- **差异点**：
	- 抽象层深度不同（轻量 API 到重编排框架）；
	- 对自主性与可控性的默认取舍不同；
	- 对调试透明度、生态集成和工程复杂度的平衡点不同。

### 6) 当前选型结论

结合本周进展，我现阶段继续以 **AutoGen** 为主线最合适：
- 已有可运行闭环，迭代成本最低；
- 能兼顾单 Agent 实验与后续多 Agent 扩展；
- 便于把精力集中在“工具协议稳定性 + 评估体系 + 可观测性”这三件核心问题上。

---

## 七、下周计划

1. 引入 RAG 评估（先小样本）：`faithfulness`、`answer relevancy`、`context precision/recall`。
2. 增强可观测性：记录每轮工具调用、耗时、重试与 token 成本。
3. 完成一次“传统 RAG vs replace_rag”的对照实验（正确率/时延/成本/可解释性）。

---

## 八、本周一句话总结

本周完成了从“概念学习”到“工程落地”的关键跨越：
**基于 AutoGen 跑通了 replace_rag 的最小可用闭环，并验证了 Agentic Retrieval 在论文问答场景下的可行性。**
